version: "3"

services:
  test:
    image: "yeasy/simple-web:latest"
    container_name: "test"
    networks: 
      - "nginx"
    environment:
      - VIRTUAL_HOST="oxiliatest.ddns.net" 
      - LETSENCRYPT_HOST="oxiliatest.ddns.net"
      - VIRTUAL_PORT=80
networks:
  nginx:
    external: true