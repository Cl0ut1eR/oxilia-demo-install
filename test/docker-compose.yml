version: "3"

services:
  test:
    build: .
    image: "mytest"
    container_name: "test"
    restart: "always"
    networks: 
      - "nginx"
    environment:
      - VIRTUAL_HOST="oxiliatest.ddns.net" 
      - LETSENCRYPT_HOST="oxiliatest.ddns.net"
networks:
  nginx:
    external: true